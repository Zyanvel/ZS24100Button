let container = document.querySelector('#container');

for (let repeat = 0; repeat < 100; repeat++) {
    const buttons = document.createElement('button');
    buttons.innerText = 'Doeeeeeey! xD';
    container.appendChild(buttons);
};